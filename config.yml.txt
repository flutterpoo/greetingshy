# FluttershyGreetings config
# Everything here is plain text and color codes (the & symbol) — reword it,
# add phrases, or delete them freely. {player} gets replaced with whoever
# joined or left. One phrase from each list is picked at random every time,
# so it won't feel repetitive. Restart the server (or /reload confirm)
# after making changes.

join-border: "&d~*~*~*~ &f✿ &d~*~*~*~"
join-face: "(◕‿◕)"
join-phrases:
  - "&f{player} &7drifted in on a gentle breeze"
  - "&f{player} &7tiptoed into the meadow"
  - "&f{player} &7peeked out from behind a leaf"
  - "&f{player} &7landed softly, like a butterfly"
  - "&f{player} &7arrived, quiet as morning dew"

leave-border: "&d~*~*~*~ &f✿ &d~*~*~*~"
leave-face: "(._.)"
leave-phrases:
  - "&f{player} &7slipped quietly back into the bushes"
  - "&f{player} &7fluttered off into the sunset"
  - "&f{player} &7went to go hide somewhere cozy"
  - "&f{player} &7drifted away on the wind"
  - "&f{player} &7tucked in for a little nap"
